# Required by deploy/compose.yaml.
# Copy this file to .env and set every value. Empty values are rejected.
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=

# Optional variables
# None are currently consumed by the local infrastructure baseline.
